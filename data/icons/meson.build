icondir = catalogue_datadir / 'icons' / 'hicolor'

install_data(
  '@0@.svg'.format(application_id),
  install_dir: icondir / 'scalable' / 'apps',
)

install_data(
  '@0@-symbolic.svg'.format(application_id),
  install_dir: icondir / 'symbolic' / 'apps',
)